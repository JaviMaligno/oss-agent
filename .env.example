# OSS Agent Environment Variables

# Required: Anthropic API key for Claude
ANTHROPIC_API_KEY=your-api-key-here

# Optional: Override default mode (oss or b2b)
# OSS_AGENT_MODE=oss

# Optional: Enable verbose logging
# OSS_AGENT_VERBOSE=true

# Optional: Custom data directory (default: ~/.oss-agent)
# OSS_AGENT_DATA_DIR=/path/to/data

# Optional: Jira configuration (for B2B mode)
# JIRA_BASE_URL=https://your-org.atlassian.net
# JIRA_EMAIL=your-email@example.com
# JIRA_API_TOKEN=your-jira-token

# Optional: Linear configuration (for B2B mode)
# LINEAR_API_KEY=your-linear-key
